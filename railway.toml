[build]
builder = "nixpacks"

[deploy]
startCommand = "node --experimental-strip-types apps/operations/server.ts"
healthcheckPath = "/api/public/contact"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
